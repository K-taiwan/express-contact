1. npm init
2. git init
3. create .gitignore
4. npm i express mongoose body-parser
5. run nodemon

must serve the public directory(this helps get the app.js and main.css)  //app.use(express.static(`${__dirname}/public`));//

